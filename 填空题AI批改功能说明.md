# 填空题AI批改功能说明

## 功能概述

新增了填空题AI批改功能，AI会根据参考答案对学生的填空题答案进行自动评分。

## 核心特点

### 1. 以参考答案为准
- AI严格按照题库中的参考答案进行评分
- 不接受近似答案或同义词
- 确保评分的一致性和公平性

### 2. 智能分隔符处理
- 自动识别顿号（、）和逗号（,）
- 统一处理不同的分隔符格式
- 支持混合使用分隔符

### 3. 按空格精确计分
- 每个空格平均分配分数
- 逐个比对每个空格的答案
- 答对几个空格得几个空格的分数

## AI批改规则

### 1. 精确匹配原则
```
参考答案：CPU、内存、硬盘
学生答案：CPU、内存、硬盘  ✅ 满分
学生答案：处理器、内存、硬盘  ❌ 部分分（只有内存对）
```

### 2. 大小写不敏感
```
参考答案：CPU、RAM、ROM
学生答案：cpu、ram、rom  ✅ 满分
学生答案：Cpu、Ram、Rom  ✅ 满分
```

### 3. 空格自动处理
```
参考答案：北京、上海、广州
学生答案： 北京 、 上海 、 广州   ✅ 满分（自动去除前后空格）
```

### 4. 分隔符兼容
```
参考答案：114,3.5,-3
学生答案：114、3.5、-3  ✅ 满分（分隔符统一处理）
```

### 5. 按空格计分
```
题目总分：10分，3个空格
每空分数：10 ÷ 3 = 3.3分

参考答案：北京、上海、广州
学生答案：北京、深圳、广州
结果：答对2个空格 = 3.3 × 2 = 6.6分
```

## 专用AI提示词

### 填空题专用提示词
```
你是一位专业的教师，负责批改学生的填空题答案。请根据以下要求进行评分：

1. 精确匹配：填空题需要精确匹配参考答案，以参考答案为准
2. 分隔符处理：答案可能使用顿号（、）或逗号（,）分隔，视为等同
3. 大小写不敏感：CPU 和 cpu 视为相同答案
4. 空格处理：忽略前后空格，但保留中间空格
5. 按空格计分：每个空格平均分配分数，答对几个得几个的分数

评分规则：
- 每个空格分数 = 总分 ÷ 空格数量
- 逐个比对每个空格的答案
- 完全匹配才得分，近似答案不得分
- 多填或少填的空格不得分

请以JSON格式返回结果:
{
    "score": 分数(整数或小数),
    "feedback": "AI评语:答对空格:[具体列出] 答错空格:[具体列出] 得分详情:[每空得分情况]"
}
```

## 使用方法

### 1. 启用AI批改
在教师控制面板的"设置测试参数"中：
1. 选择"简答题批改"为"AI批改"
2. 确保AI配置正确（显示绿色状态）
3. 保存设置

### 2. 学生答题
学生正常答题，系统会自动调用AI进行批改。

### 3. 查看结果
在测试结果页面可以看到：
- AI评分结果
- 详细的评分反馈
- AI批改标识

## 评分示例

### 示例1：完全正确
```
题目：请填写计算机的三大核心部件
参考答案：CPU,内存,硬盘
学生答案：CPU、内存、硬盘
题目分值：9分

AI评分：
- 得分：9分
- 反馈：AI评语:答对空格:[CPU、内存、硬盘] 答错空格:[] 得分详情:[第1空3分、第2空3分、第3空3分]
```

### 示例2：部分正确
```
题目：请填写计算机的三大核心部件
参考答案：CPU,内存,硬盘
学生答案：处理器、内存、硬盘
题目分值：9分

AI评分：
- 得分：6分
- 反馈：AI评语:答对空格:[内存、硬盘] 答错空格:[处理器(应为CPU)] 得分详情:[第1空0分、第2空3分、第3空3分]
```

### 示例3：数学填空
```
题目：解方程 x² - 4 = 0，x = ___，x = ___
参考答案：2,-2
学生答案：2、-2
题目分值：4分

AI评分：
- 得分：4分
- 反馈：AI评语:答对空格:[2、-2] 答错空格:[] 得分详情:[第1空2分、第2空2分]
```

### 示例4：格式错误
```
题目：请填写三个直辖市
参考答案：北京,上海,天津
学生答案：北京、上海
题目分值：6分

AI评分：
- 得分：4分
- 反馈：AI评语:答对空格:[北京、上海] 答错空格:[缺少第3空] 得分详情:[第1空2分、第2空2分、第3空0分]
```

## 技术实现

### 1. AI服务调用
```python
# 调用AI批改服务
success, ai_result = ai_service.grade_answer(
    question=question.content,
    reference_answer=question.correct_answer,
    student_answer=answer,
    max_score=question_score,
    question_type='fill_blank'  # 指定题目类型
)
```

### 2. 结果存储
填空题的AI批改结果存储在 `ShortAnswerSubmission` 表中：
- `grading_method`: 'ai'
- `score`: AI评分
- `comment`: AI反馈
- `ai_original_score`: AI原始评分
- `ai_feedback`: AI详细反馈

### 3. 结果显示
在测试结果页面，填空题会显示：
- 具体得分（而不是对/错）
- AI批改标识
- 详细的评分反馈

## 配置要求

### 1. AI服务配置
确保 `config.py` 中的AI配置正确：
```python
AI_GRADING_CONFIG = {
    'enabled': True,
    'api_key': 'your-api-key',
    'base_url': 'https://api.deepseek.com',
    'model': 'deepseek-chat',
    # ...
}
```

### 2. 测试设置
在教师控制面板选择"AI批改"模式。

## 注意事项

### 1. 答案格式要求
- 参考答案必须使用标准格式
- 建议使用顿号（、）分隔多个答案
- 避免在答案中使用不必要的空格

### 2. AI评分特点
- AI严格按参考答案评分，不接受近似答案
- 适合有标准答案的填空题
- 不适合开放性或有多种正确答案的题目

### 3. 成本考虑
- AI批改会消耗API调用次数
- 建议合理使用，避免不必要的成本

### 4. 人工复核
- 教师可以对AI评分进行人工复核
- 支持修改AI给出的分数和评语
- 复核后会标记为"已复核"

## 与简答题AI批改的区别

| 特性 | 填空题AI批改 | 简答题AI批改 |
|------|-------------|-------------|
| 评分方式 | 精确匹配参考答案 | 语义理解和要点分析 |
| 答案灵活性 | 严格按参考答案 | 接受合理的不同表述 |
| 计分方式 | 按空格平均计分 | 按要点覆盖度计分 |
| 适用场景 | 标准答案的填空题 | 开放性简答题 |
| 评分精度 | 高精度，一致性强 | 相对灵活，理解性强 |

## 更新日志

### v1.1.0 (2025-12-12)
- ✅ 新增填空题AI批改功能
- ✅ 专用的填空题AI提示词
- ✅ 精确匹配评分规则
- ✅ 按空格计分逻辑
- ✅ 分隔符兼容处理
- ✅ 结果存储和显示

---
**最后更新**: 2025-12-12  
**版本**: v1.1.0