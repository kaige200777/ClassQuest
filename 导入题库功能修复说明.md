# 导入题库功能修复说明

## 问题描述
导入题库功能出现错误：`{"message":"未找到上传文件","success":false}`

## 问题原因
前端HTML表单和后端API之间的文件字段名称不匹配：

### 前端表单字段
```html
<input type="file" id="csvFileInput" name="csv_file" accept=".csv,.xlsx" style="display:none" required>
```
- 字段名称：`csv_file`

### 后端API检查（修复前）
```python
if 'file' not in request.files:
    return jsonify({'success': False, 'message': '未找到上传文件'}), 400

file = request.files['file']
```
- 检查字段：`file`

## 修复方案

### 修复后端代码
将后端API中的文件字段名称从`file`改为`csv_file`：

**修复前：**
```python
if 'file' not in request.files:
    return jsonify({'success': False, 'message': '未找到上传文件'}), 400

file = request.files['file']
```

**修复后：**
```python
if 'csv_file' not in request.files:
    return jsonify({'success': False, 'message': '未找到上传文件'}), 400

file = request.files['csv_file']
```

## 字段对应关系

### 完整的表单字段映射
| 前端字段名 | 后端获取方式 | 用途 |
|-----------|-------------|------|
| `csv_file` | `request.files['csv_file']` | 上传的题库文件 |
| `question_type` | `request.form.get('question_type')` | 题目类型 |
| `bank_name` | `request.form.get('bank_name')` | 题库名称 |

### 其他相关功能
图片上传功能使用不同的字段名，保持不变：
```python
# 图片上传 - 使用 'file' 字段（正确）
@app.route('/api/upload_image', methods=['POST'])
def upload_image():
    if 'file' not in request.files:
        # ...
    file = request.files['file']
```

## 修复效果

### 修复前
- 导入题库时出现"未找到上传文件"错误
- 无法正常导入CSV或Excel文件
- 功能完全不可用

### 修复后
- ✅ 可以正常选择和上传文件
- ✅ 后端正确接收文件数据
- ✅ 导入功能恢复正常
- ✅ 支持CSV和Excel格式

## 导入流程

### 正确的导入流程
1. **用户操作** → 点击"导入题库"按钮
2. **文件选择** → 弹出文件选择对话框
3. **文件上传** → 选择文件后自动提交表单
4. **后端处理** → 接收csv_file字段的文件数据
5. **文件解析** → 根据扩展名解析CSV或Excel
6. **数据导入** → 将题目数据保存到数据库
7. **结果返回** → 返回导入成功或失败信息

### 支持的文件格式
- **CSV文件** (.csv)
- **Excel文件** (.xlsx, .xls)

## 相关文件
- `app.py` - 修复import_questions函数的文件字段检查
- `templates/teacher_dashboard.html` - 前端表单（无需修改）

## 技术细节

### Flask文件上传处理
```python
# 检查文件是否存在
if 'csv_file' not in request.files:
    return jsonify({'success': False, 'message': '未找到上传文件'}), 400

# 获取文件对象
file = request.files['csv_file']

# 检查文件名
if file.filename == '':
    return jsonify({'success': False, 'message': '未选择文件'}), 400
```

### HTML表单配置
```html
<form id="importForm" action="..." method="post" enctype="multipart/form-data">
    <input type="file" name="csv_file" accept=".csv,.xlsx" required>
    <input type="hidden" name="question_type" value="...">
    <input type="hidden" name="bank_name" value="...">
</form>
```

## 测试验证
- ✅ 后端正确检查csv_file字段
- ✅ 前端表单使用csv_file字段名
- ✅ 字段对应关系正确
- ✅ 不影响其他文件上传功能

## 修复时间
2024年12月16日

## 状态
✅ 已修复并测试通过